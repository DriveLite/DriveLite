{
  "name": "drivelite-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/desktop",
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "build:web": "bun --filter \"web\" build"
  }
}
